# Using Python as a Calculator:

# We can use python as a Calculator by typing "python" + enter on the terminal. This opens REPL (Repeat Evaluate Print Loop). To exit REPL type exit() on the terminal and press enter.