# Function with Arguments: A function can accept some values it can work with. We can put these values in the parenthesis. A function can also return a value as shown below.

def greet(name):
    gr = "Hello, "+name
    return gr

print(greet("Angela Yu"))